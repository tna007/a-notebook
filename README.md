## Practice redux hooks, thunk (Part 1)

- Using redux hooks in replace with `matchDispatchToProps()` and `matchStateToProps()`
- First practice in redux to fetch data from local json using `axios.get()`
